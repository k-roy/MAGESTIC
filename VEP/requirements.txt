# VEP Pipeline Requirements
# =========================
#
# Install with: pip install -r requirements.txt
#
# For GPU models (Evo2, Yorzoi, Shorkie, ESM1-v), additional
# dependencies are managed via conda environments or containers.

# Core dependencies
pandas>=1.5.0
numpy>=1.21.0
biopython>=1.79

# Optional: for alignment-based variant extraction
# mappy>=2.24  # Uncomment if using alignment_variants.py

# Optional: for running unit tests
pytest>=7.0.0
pytest-cov>=3.0.0

# Snakemake (for workflow orchestration)
# Install separately: conda install -c bioconda snakemake>=8.0
